/// <reference types="Cypress" />

ddescribe('Hooks', () => {
    before(() => {
         // runs once  befor all tests
        
    })
  
   
    after(() => {
      // runs once after all tests in the block
    })
  })